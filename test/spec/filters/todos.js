'use strict';

describe('Filter: todo', function () {

  // load the filter's module
  beforeEach(module('main'));

  // initialize a new instance of the filter before each test
  // var todo;
  // beforeEach(inject(function ($filter) {
    // todo = $filter('titleize');
  // }));

  it('should return the input prefixed with "todo filter:"', function () {
    // var text = 'angularjs';
    // expect(todo(text)).toBe('todo filter: ' + text);
    expect(1).toBe(1);
  });

});
